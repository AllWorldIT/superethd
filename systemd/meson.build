
install_data(
    'superethd.service',
    install_dir: join_paths(get_option('prefix'), 'lib', 'systemd', 'system')
)

install_data(
    'superethd@.service',
    install_dir: join_paths(get_option('prefix'), 'lib', 'systemd', 'system')
)
